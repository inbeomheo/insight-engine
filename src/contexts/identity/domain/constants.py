"""Identity & Access BC 도메인 상수.

도메인 모델 내부에서 사용하는 매직 넘버를 한곳에 모은다.
운영 환경에서 값을 바꾸려면 인프라 계층(설정/환경변수)이 본 상수를
**런타임에 한 번** 갱신하는 방식이 권장된다 (도메인이 환경변수를 직접
읽으면 순수성을 해친다).

Phase 5+에서 `IConfigGateway` 같은 포트로 외부화 예정.
"""

from __future__ import annotations


# ---------------------------------------------------------------------------
# UsageQuota
# ---------------------------------------------------------------------------

DEFAULT_DAILY_LIMIT: int = 20
"""사용자 1인당 일일 무료 사용량 기본 한도.

기존 `services/data/supabase_service.MAX_USAGE_COUNT`와 일치.
Phase 5에서 플랜별 가변치로 확장 예정.
"""


# ---------------------------------------------------------------------------
# CreditBalance
# ---------------------------------------------------------------------------

DEFAULT_INITIAL_CREDITS: int = 0
"""신규 가입 시 기본 크레딧 잔액 (충전 전)."""


__all__ = [
    "DEFAULT_DAILY_LIMIT",
    "DEFAULT_INITIAL_CREDITS",
]
